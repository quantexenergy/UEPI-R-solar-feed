# UEPI-R-solar-feed

This repository stores the public JSON feed for the UEPI-R solar data published by Quantex Energy.

It was empty; adding a sample feed and a minimal CI workflow to ensure the repository is usable by CI jobs that clone it.
